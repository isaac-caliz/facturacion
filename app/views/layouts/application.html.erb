<!DOCTYPE html>
<html>

<head>
    <title>
        <%= content_for(:title) || "Facturacion" %>
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
        <%= csp_meta_tag %>

            <%= yield :head %>

                <link rel="manifest" href="/manifest.json">
                <link rel="icon" href="https://cdn.icon-icons.com/icons2/2072/PNG/512/browser_internet_page_security_web_webpage_website_icon_127036.png" type="image/png">
                <link rel="apple-touch-icon" href="/icon.png">
                <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
                    <%= javascript_importmap_tags %>
</head>

<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link " href="/productos">
                        <i class="fas fa-clipboard-list"></i> Productos
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/proveedores">
                        <i class="fas fa-user-plus"></i> Proveedores
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/vendedores">
                        <i class="fas fa-users"></i> Vendedores
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/clientes">
                        <i class="fas fa-user"></i> Clientes
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link " href="/ventas">
                        <i class="fas fa-shopping-cart"></i> Ventas
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link " href="/ventadetalles">
                        <i class="fas fa-receipt"></i> Detalle Ventas
                    </a>
                </li>
                <li>
                    <% if user_signed_in? %>
                        <%= link_to "Sign out" , destroy_user_session_path, data: { turbo_method: :delete } %>
                            <% else %>
                                <%= link_to "Log in" , new_user_session_path %>
                                    <% end %>

                </li>
                <li>
                    <!-- Redireccion y cierre de sesion con un boton -->
                    <!-- <%= button_to( "Log Out" , destroy_user_session_path, method: :delete ) %> -->
                </li>
            </ul>
        </div>
    </nav>
    <% if notice.present?%>
        <div class="alert alert-success text-center" role="alert">
            <%= notice %>
        </div>
        <% end %>

            <% if alert.present?%>
                <div class="alert alert-success text-center" role="alert">
                    <%= alert %>
                </div>
                <% end %>



                    <%= yield %>
</body>

</html>